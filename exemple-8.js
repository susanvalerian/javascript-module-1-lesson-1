const companyName = `Guvernul Romaniei`;
const repairBoots = 150;
const defenceBots = 50;
const total = repairBoots + defenceBots;

const message = `${companyName} has ${total} bots in stock.`;
console.log(message);
